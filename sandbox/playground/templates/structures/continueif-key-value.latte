{*
	A {continueIf} testing the key of a loop over an array with keys. Nothing in
	this file may be underlined.
*}
{varType array $meta}
<dl>
	{foreach $meta as $key => $value}
		{continueIf $key === 'lang'}
		<dt>{$key}</dt>
		<dd>{$value}</dd>
	{/foreach}
</dl>
