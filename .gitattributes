# This fixture exists to prove the lexer handles CRLF line endings. Normalising
# it on checkout would silently turn it into a duplicate of the LF fixtures.
src/test/resources/data/coverage/edge-cases/CrlfLineEndings.latte -text
