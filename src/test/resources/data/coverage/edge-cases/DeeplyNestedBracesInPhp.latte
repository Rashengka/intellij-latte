{php
	$grouped = [];
	foreach ($items as $item) {
		if ($item->isVisible()) {
			foreach ($item->getTags() as $tag) {
				if ($tag !== null) {
					$grouped[$tag] = $item;
				}
			}
		}
	}
}
<p>{count($grouped)}</p>
